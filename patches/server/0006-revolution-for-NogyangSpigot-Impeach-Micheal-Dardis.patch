From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Outfit7TSB <sunnybunnies09@naver.com>
Date: Mon, 17 Feb 2025 23:53:38 +0900
Subject: [PATCH] revolution for NogyangSpigot! Impeach Micheal Dardis!


diff --git a/build.gradle.kts b/build.gradle.kts
index c0d5c1ed35049a1034a465498a9a8598a18ebb93..ac205f2ba974580dc5a1cf95886c8b66d85cf6e5 100644
--- a/build.gradle.kts
+++ b/build.gradle.kts
@@ -24,7 +24,20 @@ abstract class MockitoAgentProvider : CommandLineArgumentProvider {
 }
 // Paper end - configure mockito agent that is needed in newer java versions
 
+repositories {
+	maven {
+        url = uri("https://jitpack.io")
+    }
+	
+	maven {
+        url = uri("https://bitbucket.org/kangarko/libraries/raw/master")	
+	}
+}
+
 dependencies {
+    implementation("com.github.kangarko:Foundation:6.9.17") { // 버킷 API 부트스트랩(신)
+	        exclude(group = "io.papermc.paper", module = "paper-api") // Paper-API 제거(Paper-API가 Conflict될 위험 존재)
+    } // Paper-API 제거(Paper-API가 Conflict될 위험 존재)
     implementation(project(":NogyangSpigot-API")) // 버킷-스피곳-페이퍼-레인보우 통합 API
     implementation("ca.spottedleaf:concurrentutil:0.0.3") // Paper - Add ConcurrentUtil dependency
     // Paper start
@@ -83,6 +96,12 @@ dependencies {
     // Paper end - spark
 }
 
+configurations.all {
+    resolutionStrategy {
+        force("org.yaml:snakeyaml:2.2") // Ensure the correct version is used across all dependencies
+    }
+}
+
 paperweight {
     craftBukkitPackageVersion.set("v1_21_R3") // also needs to be updated in MappingEnvironment
 }
